<%= form_with(model: preferred_email) do |form| %>
  <% if preferred_email.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preferred_email.errors.count, "error") %> prohibited this preferred_email from being saved:</h2>

      <ul>
        <% preferred_email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :subject, style: "display: block" %>
    <%= form.text_field :subject %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
